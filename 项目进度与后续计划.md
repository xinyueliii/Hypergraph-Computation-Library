# HyperComp 项目进度与后续计划

## 已集成方法总览

> 当前进入 HyperComp 统一体系的组件共 **16 个**：**1 个原生计算底座、2 个已实际运行的原生模型、13 个已实际运行统一入口的外部模型**。

| 类别 | 方法 | 统一任务 | 接入方式 | 当前状态 | 当前完成程度 |
|---|---|---|---|---|---|
| 超图计算底座 | DeepHypergraph（DHG） | `hypergraph_computation` | 原生底座 | `registered` | DHG v0.9.7 已作为仓库主代码，后续模型在其上扩展 |
| 节点分类 | HGNN | `vertex_classification` | 原生集成 | `adapted` | 本地与远程均已通过 GPU smoke、checkpoint 保存和恢复 |
| 节点分类 | HGNN+（代码名 `HGNNP`） | `vertex_classification` | 原生集成 | `adapted` | 本地与远程均已通过 GPU smoke、checkpoint 保存和恢复 |
| 目标检测 | Hyper-YOLO | `object_detection` | 独立插件 | `adapted` | 本地与远程均已通过官方 Hyper-YOLO-N checkpoint 单图统一入口推理 |
| 目标检测 | YOLOv13 | `object_detection` | 独立插件 | `adapted` | 本地与远程均已通过官方 YOLOv13-N checkpoint 单图统一入口推理 |
| 图像分类 | SoftHGNN | `image_classification` | 独立插件 | `adapted` | 本地与远程均已完成未训练 GPU smoke；没有公开 checkpoint，尚无有效分类结果 |
| 帧—事件融合 | SuperFast | `event_based_video_frame_interpolation` | 独立插件 | `adapted` | RTX 3090 上已完成 slayer CUDA 编译、官方 checkpoint 加载、上游 API 与统一入口的合成样例推理；官方数据待验证 |
| 事件增强成像 | E-HRSAI | `event_frame_synthetic_aperture_imaging` | 独立插件 | `adapted` | RTX 3090 上已用官方 checkpoint 通过统一入口完成 256→1024 合成结构 smoke；官方数据待验证 |
| 多视角三维跟踪 | E-3DTrack | `event_based_3d_feature_tracking` | 独立插件 | `adapted` | 官方 checkpoint 与官方测试序列 `airplane_1/20` 已在 RTX 3090 通过统一入口，生成有限的 `(167, 7, 3)` 预测与真值轨迹 |
| 三维网格识别/检索 | MeshNet | `mesh_shape_recognition` | 独立插件 | `adapted` | 使用维护者补发 checkpoint 和 Manifold40 单网格，经官方预处理后在 RTX 3090 统一入口推理成功 |
| 点云补全 | Hyper-PCN | `point_cloud_completion` | 独立插件 | `adapted` | 复用 visual-cu121，官方 checkpoint 对真实网格衍生局部点云完成 1024→16384 点补全并输出可视化 |
| 文本引导目标计数 | Count Anything | `text_guided_object_counting` | 独立插件 | `adapted` | 独立 cu126 环境中用官方 checkpoint 对固定图片完成 `person` 计数，输出 4 个点与结果图 |
| 多模态三维识别/检索 | PVRNet | `multimodal_3d_shape_recognition` | 独立插件 | `adapted` | 复用 visual-cu121，官方 checkpoint 对点云+12视图样例正确识别 `piano` 并导出检索特征 |
| 目标检测 | SoftHGNN-Detection | `object_detection` | 独立插件 | `adapted` | 复用 visual-cu121，YOLO11-SoftHGNN-N 已完成单图 GPU 架构 smoke；无公开 checkpoint，不作为有效检测结果 |
| 开放集多模态三维检索 | HGM2R | `multimodal_3d_object_retrieval` | 独立插件 | `adapted` | 复用 visual-cu121，以仓库自带 670 个 ESB 三模态特征完成 CMAE—超图—HGNN—检索统一入口 smoke；无公开 checkpoint |
| 帧—事件低光增强 | EvLight V11 | `event_guided_low_light_enhancement` | 独立插件 | `adapted` | 复用 visual-cu121，以快速子集 epoch 19 final compact checkpoint 对 SDSD-in `pair23/0003` 完成统一入口推理；PSNR 29.24 dB、SSIM 0.9033，非论文协议复现 |

**目前已实际运行的模型：** HGNN、HGNN+、YOLOv13-N、Hyper-YOLO-N、SuperFast、E-HRSAI、E-3DTrack、MeshNet、Hyper-PCN、Count Anything、PVRNet、HGM2R、EvLight V11，以及 SoftHGNN 分类与 SoftHGNN-Detection 的未训练 GPU smoke。

**SuperFast 当前证据边界：** 使用官方 checkpoint 和确定性合成 THU-HSEVI 结构样例完成端到端运行；这证明统一接口可用，但不等于官方数据集复现。

## 进行中的外部模型训练

- [x] 2026-08-15 在独立 AutoDL RTX 4090 实例确认 EvLight 当前活动模型与 `egretinex-dual-freq-V11.py` 等价，仅注释标点不同。
- [x] 以不复制原始文件的符号链接方式建立 SDSD-in 快速子集：16 个训练序列、1 个验证序列，训练 batch size 4，共 20 epochs。
- [x] 在 `tmux` 会话 `evlight_v11_fast` 启动 V11 训练，并保存日志、启动命令、checkpoint 目录和恢复命令。
- [x] epoch 0 已生成 `checkpoint.pth.tar` 与 `model_best.pth.tar`；单验证序列测得 SSIM 0.8671、PSNR 27.57 dB、PSNR* 30.30 dB。该结果只属于快速子集验证，不是论文协议复现。
- [x] 使用 epoch 4 的 `model_best.pth.tar` 通过上游 `TEST_ONLY + VISUALIZE` 路径完成 `pair23/0003` 单样例推理；退出码为 0，生成输入、模糊输入、光照图、预测和 GT。单样例 SSIM 0.9012、PSNR 27.47 dB、PSNR* 30.89 dB，并已完成目视检查。
- [x] 20 epochs 正常结束，最终 best 为 epoch 19：快速验证 SSIM 0.9204、PSNR 29.99 dB、PSNR* 31.42 dB；原始 best checkpoint SHA256 为 `964bbf57a9438e862d28e73d55158b5f5adea52e8fc3787d1a4134169b7952ac`。
- [x] 将 EvLight V11 以低光增强任务 worker 接入 HyperComp 统一入口，复用 `visual-cu121`，以少量 SDSD-in 数据和 epoch 19 final compact checkpoint 完成 RTX 3090 端到端推理。
- [x] 保存统一结果、五类图像产物、checkpoint 哈希、环境、耗时、峰值显存和证据边界；记录见 `docs/validation/evlight-v11-remote-adaptation-2026-08-15.md`。
- [x] 比较最终 best 与 epoch 4：固定样例统一入口 PSNR 由 27.47 提升至 29.24 dB，MSE 由 0.00179 降至 0.00119，SSIM 由 0.9044 微降至 0.9033；采用最终权重并保持统一任务接口不变。

> 说明：`source_audited` 表示源码、版本、接口和适配代码已经整理完成，不等于模型已经复现或端到端跑通。

> 项目名称：Hypergraph-Computation-Library（HyperComp）
>
> 当前日期：2026-08-15
>
> 当前阶段：已有 15 个模型路径具备远程 RTX 3090 运行证据；本轮新增 EvLight V11，复用 visual-cu121，并保留快速子集 checkpoint 的证据边界
>
> 维护方式：每完成一项，将对应的 `[ ]` 修改为 `[x]`，并附上提交号或验证记录。

## 一、当前总体状态

HyperComp 当前以 DeepHypergraph（DHG）为底座，通过统一的组件注册、任务请求、运行结果、环境记录和验收流程，逐步接入视觉、事件、多视角和三维方向的开源研究代码。

目前 15 个模型路径具有远程 RTX 3090 统一入口运行证据。SoftHGNN-Detection 与 HGM2R 均复用 `visual-remote-cu121`：前者完成 YOLO11-SoftHGNN-N 未训练架构 smoke，后者用上游自带 ESB 三模态特征走通 CMAE、超图构建、HGNN 与检索。两者均无公开 checkpoint，因此只标记为 `adapted`，不宣称有效推理或论文结果复现。EvLight V11 同样复用该共享环境，使用快速子集 checkpoint 完成单样例有效推理；该证据不等于完整 SDSD 或论文协议复现。

新增 E-HRSAI、E-3DTrack、MeshNet 和 Hyper-PCN 已完成固定版本、源码审计、任务注册、组件清单、隔离适配器和 worker 代码，四者均已使用 checkpoint 通过远程统一入口并达到 `adapted`。其中 E-3DTrack 使用官方测试序列 `airplane_1/20` 产生三维跟踪结果；其余方法的证据边界见各自验证记录。后续新组件不再以本机运行作为目标；本机仅保留 Git 工作副本和无依赖检查。

状态含义统一为：

`registered → source_audited → upstream_verified → adapted → platform_verified`

- `registered`：已经登记论文、源码和计划入口，但没有运行结论。
- `source_audited`：已经检查源码结构、许可证、环境、数据、权重和入口。
- `upstream_verified`：原项目的最小官方流程能够运行。
- `adapted`：已经接入 HyperComp 的统一请求和结果接口。
- `platform_verified`：在声明环境中通过固定样例验收，并保存完整证据。

## 二、已经完成

### 1. 仓库与项目底座

- [x] 创建 GitHub 仓库 `xinyueliii/Hypergraph-Computation-Library`。
- [x] 将 DeepHypergraph v0.9.7 放在仓库根目录，作为项目主代码底座。
- [x] 移除 DHG 与原仓库之间的嵌套 Git 关系，后续修改由本仓库独立记录。
- [x] 保存 DHG 的原始版本、提交号、许可证和来源说明。
- [x] 建立 `dhg/`、`hypercomp/`、`components/`、`docs/`、`environments/`、`tests/` 和 `skills/` 等基础目录。
- [x] 创建项目开发 skill，记录代码接入 SOP、状态门槛和验证要求。
- [x] 建立用户长期要求清单，并规定每次项目任务先读取 skill 和统一 SOP。
- [x] 将远程 RTX 3090 优先、共享环境优先、资产外置、共享环境回归和 Git bundle 远程同步规则写入项目 skill。

### 2. 统一平台基础能力

- [x] 定义统一的 `TaskRequest` 请求结构。
- [x] 定义统一的 `TaskResult` 结果结构。
- [x] 支持组件 ID、动作、输入、checkpoint、设备、随机种子和任务参数。
- [x] 支持预测结果、指标、产物、耗时、环境、警告和错误记录。
- [x] 建立组件清单与组件查询机制。
- [x] 建立统一运行分发入口。
- [x] 建立命令行入口 `python -m hypercomp run`。
- [x] 建立独立任务清单，并支持通过 `--task` 和 `--model` 选择运行路径。
- [x] 增加任务列表与任务可用模型查询入口。
- [x] 建立成功运行和失败运行的 JSON/文本产物记录。
- [x] 将缓存、运行结果和 checkpoint 排除在 Git 跟踪之外。
- [x] 建立版本化的模型—环境映射与本机 `local_safe` / `local_conditional` / `remote_recommended` 预评估规则。
- [x] 将现有 CUDA 12.1 环境扩展为 DHG/HGNN、YOLOv13、Hyper-YOLO、SoftHGNN 共用运行时。

### 3. HGNN 与 HGNN+ 第一版原生集成

- [x] 确认 HGNN 已存在于 `dhg/models/hypergraphs/hgnn.py`。
- [x] 确认 HGNN+ 在代码中对应 `HGNNP`，位于 `dhg/models/hypergraphs/hgnnp.py`。
- [x] 为 HGNN 和 HGNN+ 创建独立组件清单。
- [x] 为两个模型建立统一的节点分类任务入口。
- [x] 建立可重复生成的微型超图测试数据。
- [x] 支持 `smoke`、`train`、`evaluate` 和 `predict` 动作。
- [x] 支持 CPU 训练和推理。
- [x] 支持 CUDA GPU 训练和推理。
- [x] 支持 checkpoint 保存、加载和模型结构参数恢复。
- [x] 保存请求、环境、预测、指标、checkpoint 和结果文件。
- [x] 验证异常组件能够返回结构化错误信息。
- [x] 将 HGNN 与 HGNN+ 状态更新为 `adapted`。

### 4. 本地验证与版本记录

- [x] 确认本机 GPU 为 NVIDIA GeForce RTX 3060 Laptop GPU。
- [x] 确认可用环境为 Python 3.10.16、PyTorch 2.7.0+cu126。
- [x] HGNN/HGNN+ CPU 训练、推理及 checkpoint 恢复测试通过。
- [x] HGNN/HGNN+ GPU 训练和推理测试通过。
- [x] 组件注册、错误处理和命令行检查通过。
- [x] 写入本地适配验收记录。
- [x] 提交 `5e54996 feat: integrate HGNN family native task`。
- [x] 将提交推送到 GitHub `main` 分支。

### 5. Hyper-YOLO 与 SuperFast 代码优先接入

- [x] 固定 Hyper-YOLO `v0.1` 对应提交 `9bfdabd8b97b5ee5da04e5df30d140a9d15557c5`。
- [x] 固定 SuperFast 提交 `b4d29efc75c4a6ca07de4c5f56f555de48b65741`。
- [x] 增加可重复下载固定源码的 `scripts/fetch_components.ps1`。
- [x] 将两个源码下载到 Git 忽略的 `third_party/` 运行区，并核对本地提交号。
- [x] 完成两个项目的许可证、环境、入口、权重链接和已知风险静态审查。
- [x] 为目标检测与事件帧插值建立任务清单和默认模型。
- [x] 建立通用独立进程边界，支持请求文件、结果文件、标准输出和错误输出记录。
- [x] 编写 Hyper-YOLO 推理 worker，将检测框和渲染图片转换为统一结果结构。
- [x] 编写 SuperFast 单样例 worker，将插值帧和基本元数据转换为统一结果结构。
- [x] 完成 Python、PowerShell 和 JSON 语法检查，并保存到 `docs/validation/external-adapters-static-2026-08-09.md`。
- [x] Hyper-YOLO 复用共享环境并下载官方 Hyper-YOLO-N checkpoint。
- [x] Hyper-YOLO 通过统一入口完成实际 GPU 模型运行。
- [x] 为 SuperFast 创建 Python 3.8 隔离基础环境并记录目标依赖。
- [x] 在 Linux CUDA 编译环境完成 SuperFast 的 slayer、checkpoint 和合成样例运行；官方样例仍待补齐。

### 6. SoftHGNN 代码优先接入与 YOLOv13 实际适配

- [x] 固定 SoftHGNN 提交 `3f46b20eeb226616a8af89f321b2e1dc3ee3634f`。
- [x] 固定 YOLOv13 提交 `73289949533efac82bb5f72ec19b746618656bd2`。
- [x] 将两个固定版本加入 `scripts/fetch_components.ps1` 并下载到 Git 忽略的 `third_party/`。
- [x] 完成许可证、依赖、任务入口、数据、checkpoint 和已知问题静态审查。
- [x] 新增 `image_classification` 任务并注册 SoftHGNN 为默认模型。
- [x] 为 SoftHGNN 编写图片推理 worker，兼容上游训练脚本可能产生的多种 checkpoint 键名。
- [x] 将 YOLOv13 注册为 `object_detection` 的第二个可选模型。
- [x] 将 Hyper-YOLO 与 YOLOv13 的重复检测逻辑抽成共享 Ultralytics worker。
- [x] 完成 Python、PowerShell、JSON 与 Git 差异语法检查，记录见 `docs/validation/soft-hgnn-yolov13-static-2026-08-09.md`。
- [x] 为 SoftHGNN 在共享环境补充 einops，并完成 GPU 前向和统一入口未训练 smoke。
- [ ] 为 SoftHGNN 准备训练产生的 checkpoint 和合法 CIFAR 样例图片。
- [x] 为 YOLOv13 建立 Python 3.11.15、PyTorch 2.2.2+cu121 的 D 盘独立环境。
- [x] 固定 NumPy 1.26.4、OpenCV 4.10.0.84，并补充源码实际需要的 `huggingface_hub`。
- [x] 下载官方 YOLOv13-N checkpoint，记录大小和 SHA256，并固定仓库自带样例图片。
- [x] 使用 RTX 3060 Laptop GPU 完成原项目单图推理，识别五个人和一辆公交车。
- [x] 通过 HyperComp 统一入口完成同一权重与样例推理，保存统一结果、日志和渲染图。
- [x] 将真实运行记录保存到 `docs/validation/yolov13-local-adaptation-2026-08-10.md`，并将状态更新为 `adapted`。

### 7. 已有本地模型的远程迁移验证

- [x] 在 AutoDL 数据盘创建 `visual-remote-cu121` 共享环境，不修改 SuperFast 的旧版运行时。
- [x] 固定 Python 3.11.15、PyTorch 2.2.2+cu121、Torchvision 0.17.2+cu121 和 NumPy 1.26.4。
- [x] 完成 CUDA 张量检查并确认 RTX 3090 可用，`pip check` 无损坏依赖。
- [x] 将 YOLOv13、Hyper-YOLO、SoftHGNN 的固定源码快照部署到远程，且不保留嵌套 `.git`。
- [x] 上传并校验 YOLOv13-N、Hyper-YOLO-N 官方 checkpoint 与固定 `bus.jpg`。
- [x] HGNN 与 HGNN+ 完成远程 GPU smoke 和 checkpoint 恢复。
- [x] YOLOv13-N 完成远程统一入口单图推理，检测到 5 个 person 和 1 个 bus。
- [x] Hyper-YOLO-N 完成远程统一入口单图推理，检测到 4 个 person、1 个 bus 和 1 个 skateboard。
- [x] SoftHGNN 完成远程确定性未训练 GPU smoke，并保留非有效预测警告。
- [x] 修复原生 GPU 测试硬编码 RTX 3060 的问题，并在 RTX 3090 上通过组件注册 5 项、原生节点分类 3 项回归测试。
- [x] 保存环境 freeze、请求、结果、日志、检测渲染图片及哈希。
- [x] 将远程验证记录保存到 `docs/validation/remote-shared-visual-2026-08-11.md`。

## 三、下一阶段计划

### P0：租用并验收统一 Linux GPU 服务器

目标：用一台兼容旧 CUDA 事件项目和现代视觉/点云项目的服务器，集中完成优先方法的最小推理与统一入口适配。

- [x] 核实 SuperFast、E-3DTrack、E-HRSAI、THU-ERGB-SAI、Count Anything、MeshNet、Hyper-PCN、PVRNet、KHGNN 和 LightHGNN 的官方环境要求。
- [x] 确定首选服务器为 RTX 3090 24 GB、12–16 vCPU、64 GB 内存、500 GB NVMe、Ubuntu 22.04、Docker + NVIDIA Container Toolkit。
- [x] 确定不使用 RTX 4090 作为首台旧事件环境服务器，不让 PVRNet 的 CUDA 9 官方环境决定首台服务器型号。
- [x] 形成环境划分与租机检查清单，见 `docs/服务器租用与优先方法环境评估.md`。
- [x] 在 AutoDL 实例确认 RTX 3090 24 GB、磁盘、SSH 与 root 权限；Docker 和快照能力尚未使用。
- [x] 启动首台远程实例并记录 Ubuntu 20.04、glibc 2.31、CUDA 11.3、Python 与 PyTorch 版本；到期时间由平台账户管理。
- [x] 使用服务器现有 PyTorch 1.10.0+cu113 兼容运行时完成 slayerPytorch 编译和 SuperFast 合成最小样例；没有额外建立容器。
- [x] 复用 `event-cu113`，用 E-HRSAI 官方 checkpoint 完成统一入口合成结构 smoke，并用 E-3DTrack 官方 checkpoint 与官方测试序列完成统一入口跟踪。
- [x] 不建立 `pointcloud-cu118`；复用 `visual-cu121` 与共享 CUDA 12.1 工具链，完成 Hyper-PCN 自定义扩展和官方 checkpoint smoke。
- [x] 实测共享环境冲突后建立 Count Anything Python 3.11/cu126 兼容环境并完成单图推理。
- [ ] 为每个方法记录峰值显存、输入样例、checkpoint、命令、输出和环境 ID。
- [ ] 制作可恢复的服务器镜像或磁盘快照。

### P0：补齐 HGNN/HGNN+ 平台级验证

目标：将当前的 `adapted` 推进到 `platform_verified`，但不把平台验收混同为论文精度复现。

- [ ] 按 `environments/core-cu126.yml` 新建一个干净环境。
- [ ] 在干净环境中重新安装 HyperComp。
- [ ] 安装 `pytest` 并执行保留的 DHG 上游测试。
- [ ] 修复因为 PyTorch 版本变化产生的必要兼容问题。
- [ ] 选择一个可维护的代表性公开数据集，优先考虑 Cora。
- [ ] 为代表性数据集建立下载、缓存和数据划分说明。
- [ ] 增加真实数据训练与评估入口。
- [ ] 固定随机种子、数据划分、模型参数和预期输出。
- [ ] 连续运行至少两次，验证结果与产物的可重复性。
- [ ] 保存样例、环境、日志、指标和文件哈希。
- [ ] 补充缺少数据、错误 checkpoint、无 GPU 等失败测试。
- [ ] 更新 HGNN/HGNN+ 验收记录。
- [ ] 满足全部条件后，将状态更新为 `platform_verified`。
- [ ] 单独提交并推送这一里程碑。

### P0：确定首批外部任务的集成边界

在正式复制外部项目代码前，需要和老师确认“统一到什么程度”。建议平台统一外部接口和运行记录，但允许各项目保留自己的数据预处理、模型内部实现和独立环境。

- [ ] 和老师确认首批演示需要覆盖几个任务。
- [ ] 确认是否要求统一训练，还是以推理和演示为主。
- [ ] 确认原项目的数据预处理是否允许保留。
- [ ] 确认是否接受不同项目使用独立 Python/PyTorch/CUDA 环境。
- [ ] 确认比赛演示是否必须使用公开 checkpoint。
- [ ] 确认最终统一展示的输入、输出、指标和页面形式。
- [ ] 根据确认结果更新 `docs/integration-scope.md`。

### P0：集成 Hyper-YOLO（视觉识别与目标检测）

当前状态：`adapted`。原项目 API 与 HyperComp 统一入口均有固定单图 GPU 运行证据。

#### 源码审查

- [x] 下载 Hyper-YOLO 源码并固定具体提交号。
- [x] 检查主代码许可证，并记录权重与数据集许可证仍需随实际资产复核。
- [x] 检查 Python、PyTorch、CUDA 和自定义算子要求。
- [x] 找到训练、验证和推理入口。
- [x] 检查配置文件、硬编码路径、GPU 编号和输出格式。
- [x] 确认官方 checkpoint 发布页面；大小、格式和哈希待实际下载后记录。
- [x] 固定使用上游代码附带的 `bus.jpg` 样例图片。
- [x] 将清单状态更新为 `source_audited`。

#### 原项目验证

- [x] 确认 Hyper-YOLO 可复用 `shared-win-cu121` 环境，无需新建环境。
- [x] 完成模型和超图模块导入测试。
- [x] 使用官方 Hyper-YOLO-N checkpoint 运行一张样例图片。
- [x] 保存环境、耗时、显存、日志、哈希和检测结果图片。
- [x] 原项目路径验证通过，并继续完成统一入口适配。

#### HyperComp 适配

- [x] 定义目标检测输入清单和参数。
- [x] 编写独立 worker/适配器。
- [x] 支持 checkpoint、设备、置信度和输出目录参数。
- [x] 将检测框、类别和置信度转换为统一结果结构。
- [x] 保留原项目可视化图片和任务特有输出。
- [x] 增加缺少权重、错误输入和 worker 失败提示。
- [ ] 增加最小适配测试。
- [x] 将清单状态更新为 `adapted`。

#### 平台验收

- [x] 在声明环境中用固定样例完成端到端运行。
- [x] 验证结果 JSON、检测图片、日志和环境信息完整。
- [ ] 验证重复运行的输出稳定性。
- [ ] 保存验收证据并更新状态为 `platform_verified`。
- [ ] 单独提交并推送 Hyper-YOLO 里程碑。

### P1：集成 SuperFast（帧—事件多模态感知）

当前状态：`adapted`。已在 AutoDL Linux RTX 3090 上完成 slayerPytorch CUDA 编译、官方 checkpoint 加载、上游 API 与统一入口的合成样例推理；官方 THU-HSEVI 样例尚未下载验证。

#### 源码审查

- [x] 下载 SuperFast 源码并固定具体提交号。
- [x] 检查主代码与 slayerPytorch 许可证，并记录资产许可证仍需随下载复核。
- [x] 检查 Python、PyTorch、CUDA、自定义算子和系统依赖。
- [x] 找到推理入口、配置方式和输入格式。
- [x] 确认事件数据、相邻视频帧和时间戳的组织方式。
- [x] 确认并下载官方 checkpoint，记录大小 `418,931,023` bytes 与 SHA256 `4828b7647b77198795165d2a3c18c37c2773b4c785fd06a96da9df296965ce88`。
- [x] 创建明确标注为合成数据的 THU-HSEVI 结构兼容最小事件—帧样例；官方样例仍待取得。
- [x] 将清单状态更新为 `source_audited`。

#### 原项目验证

- [x] 创建 `D:\Anaconda\envs\hypercomp-superfast` Python 3.8.20 基础环境。
- [x] 提交 `environments/superfast-win-cu111.yml` 目标环境规格。
- [x] 在 Linux CUDA 11.3 兼容环境完成 PyTorch 1.10、slayerPytorch 编译与模型导入；未逐字复刻上游 PyTorch 1.9 环境。
- [x] 完成 CUDA 扩展调用、模型和依赖导入测试。
- [ ] 使用官方 checkpoint 运行最小官方样例。
- [x] 使用官方 checkpoint 运行合成最小样例，并保存插值帧、日志、环境和运行时间。
- [ ] 将清单状态更新为 `upstream_verified`。

#### HyperComp 适配与验收

- [x] 定义 THU-HSEVI 场景目录输入和输出结构。
- [x] 编写独立 worker/适配器。
- [x] 支持场景目录、样例编号、checkpoint 和设备参数。
- [x] 将插值帧路径、样例编号、分辨率和耗时写入统一结果。
- [x] 增加缺少权重、输入数量、样例编号和 worker 失败提示。
- [x] 增加确定性合成样例生成器并完成最小适配运行。
- [x] 使用固定合成样例完成端到端适配验证；官方样例验收仍待完成。
- [x] 保存合成样例适配证据并更新为 `adapted`；`platform_verified` 仍待官方样例、重复运行和失败路径验收。
- [ ] 单独提交并推送 SuperFast 里程碑。

### P1：验证 SoftHGNN 并补齐 YOLOv13 平台级验收

当前状态：SoftHGNN 为 `adapted`（仅未训练 smoke）；YOLOv13 为 `adapted`（官方 checkpoint 单图推理）。

- [x] 评估 SoftHGNN 并确认可复用 `shared-win-cu121`，无需 PyTorch 2.4.1 独立环境。
- [x] 安装 einops 0.8.1，默认模型 GPU 前向与统一入口 smoke 通过。
- [ ] 修正或绕开上游训练脚本的 checkpoint 保存/恢复命名不一致。
- [ ] 在 CIFAR-10 上训练得到一个可用于平台验收的 checkpoint。
- [x] 使用固定图片通过统一入口输出带“未训练权重”警告的 top-k smoke 结果。
- [ ] 训练或取得 checkpoint 后输出具有实际意义的 top-k 分类结果。
- [x] 为 YOLOv13 建立 Python 3.11、PyTorch 2.2.2 独立环境。
- [x] 使用 SDPA 回退路径，避免把 FlashAttention 作为 Windows 必需项。
- [x] 下载 YOLOv13-N 权重并记录来源、大小和 SHA256。
- [x] 使用固定图片通过统一入口输出检测 JSON 和渲染图。
- [x] 在远程复用同一固定样例，对比 Hyper-YOLO 与 YOLOv13 的统一输出。
- [ ] 重建一次已提交的 YOLOv13 环境，重复固定样例并补齐失败路径后再考虑 `platform_verified`。

### P1：补齐事件、多视角与三维展示方向

当前统一以远程 RTX 3090 为运行和验收平台，本机不再安装新模型环境。

环境采用最小数量策略：E-HRSAI、E-3DTrack、MeshNet 优先共享 `event-cu113`，Hyper-PCN 优先复用 `visual-cu121`；只有实测二进制冲突后才新增完整环境。

- [x] 比较并选择 E-HRSAI、E-3DTrack、MeshNet、Hyper-PCN 四个项目。
- [x] 固定四个上游提交并完成源码、许可证、checkpoint、输入输出和环境审计。
- [x] 建立四个任务清单、组件清单、统一适配器和远程 worker。
- [x] 完成本地无依赖语法检查和组件注册测试。
- [x] 将四个组件的代码提交、推送并同步到远程仓库。
- [x] 远程建立共享事件环境，下载 E-HRSAI 官方 checkpoint 并记录大小与 SHA256。
- [x] E-HRSAI 使用官方 checkpoint 通过统一入口完成合成结构样例推理并保存显存、耗时和图片。
- [x] 确认 MeshNet README 三个链接确实失效，并从官方 Issue 找到维护者补发 checkpoint 与可用 Manifold40 替代源。
- [x] 确认 E-HRSAI 可复用 `event-cu113`；E-3DTrack 也分配到该环境，取得资产后先做兼容验证，不预先克隆环境。
- [x] 下载并记录 E-HRSAI checkpoint；E-3DTrack Google Drive 在 AutoDL 超时，已从本机下载并转传，服务器哈希一致且 state_dict 全部匹配。
- [x] MeshNet 使用单个 Manifold40 OBJ 经官方预处理生成 NPZ，并以维护者补发 checkpoint 完成统一入口 GPU 推理和特征导出。
- [x] 下载并校验 E-3DTrack 3.3 GB 官方数据集，提取官方测试序列 `airplane_1/20`，完成统一入口最小跟踪推理并保存预测与真值轨迹。
- [x] 实测 Hyper-PCN 环境兼容性，确认无需复制 CUDA 11.8/PyTorch 环境。
- [x] 不创建独立 Hyper-PCN 环境；复用 `visual-cu121` 并补充 184 MB 共享 CUDA 12.1 编译工具链。
- [x] 编译 Chamfer 与 PointNet2 扩展，下载官方 PCN checkpoint 并完成统一入口合成点云 smoke。
- [ ] 取得合法固定 PCN 样例并完成数据预处理、指标与官方数据验收。
- [ ] 为每个真实运行保存环境、命令、输入、输出、耗时和显存证据。

### P1：Count Anything、PVRNet 与 Hyper-PCN 真实样例

- [x] 固定 Count Anything 与 PVRNet 官方源码提交。
- [x] 完成两个项目的许可证、入口、依赖、输入输出、checkpoint 和迁移风险审计。
- [x] 新增文本引导计数与多模态三维识别/检索任务清单。
- [x] 新增 Count Anything 和 PVRNet 组件清单、统一 adapter 与独立 worker。
- [x] 在 `visual-cu121` 实测 Count Anything，复现缺少 `torch.nn.attention` 后才建立 cu126 独立环境。
- [x] 下载并校验 Count Anything 官方 checkpoint，以固定图片和 `person` 查询完成统一入口计数与可视化。
- [x] 下载并校验 PVRNet 官方 checkpoint，以 Manifold40 网格生成点云+12视图固定样例。
- [x] 在现代 PyTorch 环境修复 PVRNet 单样例批次问题，完成 `piano` 分类及检索特征导出。
- [x] 为 Hyper-PCN 生成合法固定真实网格衍生局部点云，完成点云补全和输入/输出可视化。
- [x] 对共享环境执行 `pip check`，并完成 PVRNet、Hyper-PCN 聚焦运行回归和验证记录。

### P1：SoftHGNN-Detection 与 HGM2R

- [x] 将 SoftHGNN 检测端注册为 `object_detection` 的第三个可选模型。
- [x] 选择无需 FlashAttention 的 YOLO11-SoftHGNN-N 作为首个检测架构。
- [x] 在 `visual-cu121` 完成单图 GPU 架构 smoke，记录无公开 checkpoint 的证据边界。
- [x] 固定 HGM2R 官方源码并登记开放集多模态三维检索任务。
- [x] 接入上游自带 ESB 点云、体素和多视图预提取特征输入。
- [x] 走通 CMAE、超图构建、HGNN、检索排序和 embedding 导出统一入口。
- [x] 两项均复用 `visual-cu121`，没有创建组件专属环境。
- [ ] 若后续需要有效预测或论文指标，为两项训练并保存可追溯 checkpoint。

### P1：完善平台公共能力

- [x] 将运行分发从代码内固定映射改为组件清单驱动。
- [ ] 为不同任务增加可验证的输入清单 schema。
- [x] 为独立 worker 增加标准输出/错误输出捕获。
- [ ] 增加 checkpoint URL、哈希和本地缓存管理。
- [ ] 增加环境 ID 与独立环境启动机制。
- [x] 增加任务列表和任务可用模型查询命令；组件详情仍待补充。
- [ ] 增加统一运行历史索引。
- [ ] 增加结果摘要和跨任务展示接口。
- [ ] 增加 Windows 与 Linux 路径兼容测试。
- [ ] 建立基础持续集成测试。

### P2：比赛展示与材料联动

- [ ] 选择 3—4 个真正通过验收的演示任务。
- [ ] 为每个任务准备合法的小型示例数据。
- [ ] 统一展示输入、超图关系、任务输出和耗时。
- [ ] 绘制 HyperComp 总体架构图。
- [ ] 绘制超图构建与消息传播示意图。
- [ ] 绘制原生任务和独立任务的运行流程图。
- [ ] 保存每个任务的界面截图、输出图片或视频。
- [ ] 将真实实现状态同步回中英文参赛文稿。
- [ ] 将平台实测结果与论文原始结果明确区分。
- [ ] 完成最终演示脚本、README 和安装说明。

## 四、当前暂缓范围

- [x] 暂不集成大模型、RAG 或大模型推理方向。
- [x] 暂不集成攻击、防御和安全对抗方向。
- [x] 暂不要求所有项目共用同一个数据处理流程。
- [x] 默认优先扩展共享环境；只有确认 PyTorch/CUDA 或编译依赖冲突时才建立独立环境。
- [x] 暂不要求所有有源码的论文都完成论文精度复现。

## 五、推荐执行顺序

1. [x] 建立统一的任务/模型选择入口并完成四个外部项目的代码优先适配。
2. [x] 和老师确认模型统一注册，任务数据预处理和推理代码按任务保留，环境尽量共享。
3. [x] 为 YOLOv13 建立独立环境，下载 YOLOv13-N 权重并完成统一入口单图 GPU 验证。
4. [x] 为 Hyper-YOLO 复用共享环境，下载官方 checkpoint，并用 YOLOv13 固定图片完成推理。
5. [x] 完成 SoftHGNN GPU smoke；训练或准备 CIFAR-10 checkpoint 仍待后续。
6. [x] 为 SuperFast 创建隔离基础环境并完成本机可行性评估；完整环境转到 Linux 服务器。
7. [x] 通过统一入口完成 SuperFast 固定合成样例适配；官方固定样例验收仍待完成。
8. [x] 将 HGNN/HGNN+、YOLOv13、Hyper-YOLO 和 SoftHGNN 迁移到远程共享环境并完成固定路径运行。
9. [x] 完成 E-HRSAI、E-3DTrack、MeshNet、Hyper-PCN 的代码优先接入。
10. [x] 在远程复用共享环境完成 E-HRSAI 与 Hyper-PCN 的官方 checkpoint 合成样例运行。
11. [x] 找回 MeshNet 合法替代资产并在 `event-cu113` 完成单网格最小样例运行。
12. [x] 下载并校验 E-3DTrack 官方数据集，在 `event-cu113` 以官方 checkpoint 和首条官方测试序列完成端到端最小样例运行。
13. [x] 完成 Count Anything、PVRNet 与 Hyper-PCN 真实样例这一批优先工作。
14. [ ] 用真实验收结果更新比赛文稿、架构图和演示材料。

## 六、每次更新时的检查规则

- [ ] 是否只把有运行证据的内容标记为完成？
- [ ] 是否记录了对应代码提交号？
- [ ] 是否保存了环境、输入、输出、指标和错误信息？
- [ ] 是否区分论文结果、原项目结果和 HyperComp 实测结果？
- [ ] 是否避免将 `registered` 项目描述为已经集成？
- [ ] 是否将生成数据、checkpoint、缓存和运行产物排除在 Git 之外？
- [ ] 是否完成一个独立里程碑后再提交并推送？
